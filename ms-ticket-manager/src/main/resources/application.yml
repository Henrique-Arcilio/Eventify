spring:
  application:
    name: ms-ticket-manager
  data:
    mongodb:
      uri: mongodb+srv://${USERNAME}:${DB_PASSWORD}@clusterdesafio3.mpnjzen.mongodb.net/db-ticket?retryWrites=true&w=majority&appName=ClusterDesafio3
security:
  jwt:
    token:
      secret-key: "abcdefghijklmnopqrstuvwxyz123456"
      expire-length: 3600000
feign:
  event-manager:
    name: ms-event-manager
    url: http://localhost:8081/api/v1/events

springdoc:
  paths-to-match:
    - /api/v1/**
    - /auth/**